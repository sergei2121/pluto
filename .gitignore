# секреты — никогда не коммитить
.env

# node / сборка
node_modules/
dist/

# агенты: артефакты сборки
agent/pluto-agent.exe
agent/agent

# aida-monitor: артефакты сборки
aida-monitor/aida-monitor
aida-monitor/aida-monitor.exe

# системные файлы
.DS_Store
*.log
.next/
build/
